/*global define*/

define([
    'underscore',
    'backbone'
], function (_, Backbone) {
    'use strict';

    var StatsfordateModel = Backbone.Model.extend({
        defaults: {
        }
    });

    return StatsfordateModel;
});